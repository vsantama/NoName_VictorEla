export default class Game extends Phaser.Scene {
  constructor() {
    super({ key: 'main' });
  }
  preload() {  
  }//images, sprites, etc

  create() {//"poner las cosas"
  }

  update(time, delta) {    //interactions
  }
}